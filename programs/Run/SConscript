#custom build flags
warnFlags = ['-Wall', '-W']

#define build environment
env = Environment();
warnEnv = env.Clone(CXXFLAGS=warnFlags);

libs=['rt','pthread','usrp','usb','hdf5_hl_cpp','hdf5_cpp'];
warnEnv.Program(target='../../bin/gradar-run', source=Glob('*.cxx'), LIBS=libs);

